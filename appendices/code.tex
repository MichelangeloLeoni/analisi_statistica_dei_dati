\chapter{Code}

\todo{Displaying the code imho is a good idea, but, a the moment, our code is confusing and too much vibe-coded. The right thing to do is to create a little library containing the most used functions (e.g. the lr ordering) and cleaning up the overall code. This way the code should be good enough to be displayed in this text and in the github repo without making us look dumber than we already are :)}

We are not programmers. The code is not optimal. It is not supposed to be a real library for statistical analysis, only a pseudo-library to use for the notes. Other libraries serve the same use and are fare better at it, we built it to have fun and to apply the concept we studied in the course, gaining a deeper understanding of the topics in the process.

HOW WE IMPLEMENTED ORDERING FUNCTIONS
\input{code/ordering_functions_code.tex}

HOW WE IMPLEMENTED NEYMAN INTERVALS COMPUTING
\input{code/neyman_intervals_code.tex}
